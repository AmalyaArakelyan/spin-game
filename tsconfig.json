{
  "compilerOptions": {
    "moduleResolution": "node", // or "bundler" if using modern tools
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    // Ensure all your extensions are handled:
    "allowJs": true,
    "jsx": "react-native" // or "react" if web
  }
}
